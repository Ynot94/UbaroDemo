<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tonyjm.ubarodemo">

    <uses-feature android:name="android.hardware.camera2" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EnviarActivity" />
        <activity android:name=".PagarActivity" />
        <activity android:name=".RecibirActivity" />
        <activity android:name=".MainSendQuantityActivity" />
        <activity android:name=".ConfirmarEnvioActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AgregarORetirar" />
        <activity android:name=".MetodoParaCargarActivity" />
        <activity android:name=".EnviarPrincipal" />
        <activity android:name=".CargarCuantoActivity" />
        <activity android:name=".ConfirmarRecargaActivity" />
        <activity android:name=".SimulatePaymentActivity" />
    </application>

</manifest>